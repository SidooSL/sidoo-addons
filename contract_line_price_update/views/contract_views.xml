<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="contract_update_action_server" model="ir.actions.server">
        <field name="name">Update Contract Line Prices</field>
        <field name="groups_id" eval="[(4, ref('contract_line_price_update.group_contract_price_update'))]"/>
        <field name="model_id" ref="contract.model_contract_contract"/>
        <field name="binding_model_id" ref="contract.model_contract_contract" />
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_open_price_update_wizard()
        </field>
    </record>
</odoo>
